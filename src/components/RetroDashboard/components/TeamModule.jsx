import React, { useState } from 'react';

export const TeamModule = () => {
  const [selectedRole, setSelectedRole] = useState(null);
  
  const roles = {
    // ... roles data ...
  };

  return (
    // ... existing TeamModule JSX ...
  );
}
